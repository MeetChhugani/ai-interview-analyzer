name: frontend
description: "AI Smart Interview Analyzer frontend application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  camera: ^0.10.5+9
  record: ^5.1.2
  path_provider: ^2.1.3
  fl_chart: ^0.68.0
  google_fonts: ^6.2.1
  flutter_tts: ^3.8.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  record_platform_interface: 1.1.0
